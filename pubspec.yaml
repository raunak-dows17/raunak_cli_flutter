name: raunak_cli
description: A Dart CLI to generate Flutter projects with a clean architecture setup, folder structure, and essential boilerplate.
version: 1.0.1

repository:  https://github.com/raunak-dows17/raunak_cli_flutter.git

environment:
  sdk: ^3.7.1

dependencies:
   path: ^1.9.1
   args: ^2.7.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.26.2

executables:
  raunak: raunak
